# TrafficBook PostgreSQL Configuration
# Per-region isolated DB — no replication needed

listen_addresses = '*'
max_connections = 100
shared_buffers = 256MB

# Logging
log_timezone = 'UTC'
timezone = 'UTC'
